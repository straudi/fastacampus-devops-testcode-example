{
  "name": "calculator",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "reporters": [
      "default",
      [
        "jest-junit",
        {
          "outputDirectory": "./coverage",
          "outputName": "jest-junit.xml"
        }
      ]
    ]
  },
  "dependencies": {
    "jest-junit": "^16.0.0"
  }
}
